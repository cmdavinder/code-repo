server:
  port: 8003
  
---  
spring:
  profiles: dev
  application:
    name: edge-server
  cloud:
    config:
      fail-fast: true
      discovery:
        enabled: true
        serviceId: config-server
info:
  tlast: -1
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8002/eureka/